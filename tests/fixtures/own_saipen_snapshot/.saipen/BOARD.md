## DOING
## TODO
## DONE
- [x] T-528 P1 protocol baseline drift: viewer BASELINE_VERSION 7.223.0, canonical repo now 7.223.5 -- test_protocol_sync::test_baseline_matches_the_repo_when_reachable FAILS (assert '7.223.0' == '7.223.5', tests/test_protocol_sync.py:304). Re-read vocabularies in saipenview/protocol.py and bump BASELINE_VERSION | verify: test_baseline_matches_the_repo_when_reachable green with clean-clone SAIPEN_HOME; protocol_sync 24/24 @7.223.5 | owner: buffy | claim_time: 2026-08-12T04:05:00Z
- [x] T-530 P2 clipboard security cluster: (a) api.py:1202-1207 Set-Clipboard builds a PowerShell -Command string doubling double-quotes but never escaping $() or backticks -- a copied string containing $(...) executes arbitrary PS (text is user-controlled clipboard content); (b) same subprocess.run has no timeout -- a hung powershell blocks the RPC; (c) service.py SSE carries the session token in the query string (documented) -- leaks into URL logs/history, low severity on loopback | verify: clipboard_copy escapes PS metacharacters or uses a non-shell mechanism; timeout added; SSE token moved to header if feasible | owner: buffy | claim_time: 2026-08-12T04:05:00Z
- [x] T-531 P2 README api-count doc drift: README.md:350 claims api.py has 77 public methods; actual non-underscore defs = 87 (104 total). WIKI-013 verified 85 @71ed3a5 -- the count has drifted twice since T-181's correction | verify: README architecture line matches a fresh count | owner: buffy | claim_time: 2026-08-12T04:05:00Z
- [x] T-532 P2 test_sessions flake recurring: TestRoundTrip::test_last_run_is_the_newest failed in full suite (run_id ordering, aider vs gemini), passes in isolation 0.06s. E-492 documented the same flake once before -- second occurrence, still unhunted | verify: 3 consecutive full-suite runs green; root cause documented in ticket | owner: buffy | claim_time: 2026-08-12T04:04:59Z
- [x] T-533 P3 invisible service-mode WIP: 12 uncommitted files (run.bat, __main__.py, app.js, index.html, service.py, 4 transport JS, 3 tests) excluded from 3 releases by the dirty-tree rule (E-482/E-490) with NO BOARD ticket owning them -- the work is real, shipped around, and invisible to planning | verify: service-mode WIP lands under a ticket (commit) or is explicitly abandoned | owner: buffy | claim_time: 2026-08-12T03:57:02Z
- [x] T-534 P3 own-.saipen WARN stack normalized: canonical validate.py reports 44 WARNs accepted as known across releases -- board-soft-cap (32 KB vs ~16 KB, 27 KB closed-ticket text), half-claim (T-195 TODO, T-203 DONE carry owner without claim_time), changelog-unarchived (17 entries vs stated ~10), legacy sealed LOG-001 taxonomies/timestamp inversions. Familiarity blindness: known has become permanent | verify: WARN count reduced or each WARN class has an owning ticket | owner: buffy | claim_time: 2026-08-12T03:47:02Z
- [x] T-204 P1/P2 canonical-writer hardening wave 2 (T-203 continuation, unshipped since v0.1.28). P0#2/#3: coordinator mutate contract (planner returns only OperationPlan/refusal; APPLY + one finalize_self_writes in coordinator; mutate_doc hand-edits verification_policy=none; commit-time proof revalidated under lock). P0#4: canonical ID authority -- local next_event_id/next_ticket_id deleted, delegated to saio.next_ticket_id/next_event_id (scans sealed logs, excludes T-998/999); saio.engine loads saipen_engine.log. P0#5: recovery-truth -- post-journal exception inspects pending_ops -> RECOVERY_REQUIRED/recovery_required. P1#6: manual-work operation_id minted pre-RPC end-to-end (committed 48485c6). P1#7/#8: ExternalChangeRegistry canonicalized keys; consume_self_write deleted (external violation survives bytes-identical writes until explicit ack). P1#9/#10: one strict ticket-line grammar shared by display + conformance (duplicates/malformed surfaced, escaped pipes, blocker renders exactly); _strict_frontmatter refuses duplicate authority keys. P1#11: real subprocess lock exclusion (_LOCK_HOLDER_CHILD). P2#13: freshness wrappers delegate to canonical saio.source_identity/role_revision/generic_role_revision. Python 3.10 CI #42 fixed (Path.stat interception version-fragile). Shutdown fd-crash (T-179 family) root-caused + fixed: test_scanner no real-drive scans (tmp_path/scan_roots=[]), git subprocess timeouts, scanner bounded pool join -- full suite 949 passed x3 clean (E-481). | verify: full suite 949 passed x3 (3.11) + 925 passed (3.10 venv310) with clean-clone SAIPEN_HOME; ruff check + format + compileall + node --check clean; canonical validate.py PASS on own .saipen + regenerated snapshot; protocol_sync 24/24 @7.223.0; release_gate PASS at 0.1.29 | owner: opencode | claim_time: 2026-08-11T18:10:00Z
- [x] T-203 P0 repair mission (canonical writer authority, user instruction 11.08.26). Every .saipen mutation now commits through the canonical SAIPEN writer pipeline (saio.py -> saipen_engine: OS writer lock, PREPARED journal, ordered targets, byte+semantic verify, recovery) -- the fake-atomic multi-file CAS is gone; decisions are bound to the exact snapshot whose hashes are the plan preconditions (STALE_STATE on drift). Ticket lifecycle delegates to canonical claim/ticket_move/finish (viewer cannot manufacture DONE; SHIP gate enforced). Collect: strict OUTBOX parser (duplicates/junk critical = MALFORMED), package gate -> immutable proof, boundary check (backend external-change registry + recovery), collect_policy from charter, proof revalidated under the lock at commit (STALE_FRESHNESS). External-change state moved backend-side ((root, rel) registry, survives selection/switches, blocks collect). Manual-work op-id is end-to-end (UI mints, API validates, parser persists [op: id]). Staleness is content-based; fingerprint typed (MISSING != FILE\0hash). One structured mutation-result contract (ok/code/message/changed_files/retryable/recovery_required). | verify: full suite 925 passed @30684e9+worktree with clean-clone SAIPEN_HOME; test_saio_writer 11/11 (crash-after-every-target, recovery idempotent, cross-process lock, alias, stale-decision); test_mission_p1 20/20 (external registry, op-id, OUTBOX strictness, content staleness, typed fingerprint); canonical validate.py PASS on own .saipen + snapshot; ruff check + format + compileall + node clean; protocol_sync 24/24 @7.223.0; E-474..E-476
- [x] T-202 P0 repair mission vs canonical SAIPEN (user instruction 11.08.26). protocol.py synced to 7.223.0 + vocab expanded (commands, ticket fields, handoff fields, intents, log taxonomies, clock slack); protocol_sync re-pointed at saipen_engine homes (old sync TypeErrored on 7.2xx); conformance enforces blocker invariant / DONE-verify / last_event==LOG tail / style_contract@v3 / one-sentence WAIT / Pick Rule / canonical DONE-WAIT wordings; move_ticket strict state machine; collect package gate (freshness+role+identity, fail-closed); atomic single-writer ownership (launch-vs-mutation, cross-doc deps); manual-work op-id idempotency; release gate evidence-strict + tools-only CI; own-.saipen CI snapshot split; green-baseline fixtures | verify: full suite 926 passed @83cdd43 with clean-clone SAIPEN_HOME (CI layout); protocol_sync 24/24 @7.223.0; canonical validate.py PASS on own .saipen + tracked snapshot; ruff check + format + compileall + node --check clean; release identity PASS (0.1.27); E-469..E-471
- [x] T-193 P2 test-surface hardening (mission K.9, partially done). The full-suite flake family is fixed at the root (event-bus leak + stdout guards + deterministic watcher tests), and CI now clones the canonical SAIPEN validator (SAIPEN_HOME) so the own-`.saipen` conformance rule-fire tests gate the build; verify the CI wiring on a real runner. | verify: CI windows job runs test_conformance_legacy (rule-fire tests, memory-dependent ones skip when .saipen absent by T-172); a deliberate board corruption fails CI -- DONE: CI wiring verified on a real runner across 3 pushes. 3.10 collection fixed via tomli fallback (dev extra + CI `-e ".[dev]"`); release_gate newest-tag test skips on shallow checkouts (no tags -> nothing to be behind); T-199 grandfathered fix green. Final CI @83cdd43: 4 failed / 811 passed / 3 skipped, the 4 = known foreign protocol_sync BASELINE drift (canonical 7.222 vs viewer 7.210; E-439 foreign WIP), not this ticket. Board-corruption red-control not exercised (intentional corruption = destructive, out of scope). E-465..E-466
- [x] T-199 P2 conformance grandfathered-marker test stale post-CLEAN (HUNT 08.08.26). tests/test_conformance_legacy.py::test_grandfathered_marker_is_uniform asserts `\| verify: grandfathered` markers exist on BOARD.md, but CLEAN (E-435, 20:51) pruned the 111 legacy DONE tickets that carried them -- the marker count is now zero, so the test fails permanently. Known since E-439 but unticketed. Decide: update the test to assert the policy doc instead of live board markers, or restore a single exemplar marker. | verify: test_grandfathered_marker_is_uniform passes; docs/conformance-legacy.md policy unchanged -- DONE: test now asserts the single canonical format from docs/conformance-legacy.md (Treatment 3) and checks any live board marker against it; a legitimately-empty board no longer fails. 4/5 in file green (sole FAIL = known test_own_saipen_memory validator pair), ruff check+format clean, diff test-only. E-458..E-460
- [x] T-196 P2 stale backup debris: saipenview/ui/static/style.css.bak + style.css.bak.stale are untracked, gitignored backup files. .bak.stale is by-convention stale (Wintage-era leftover); style.css.bak was the T-142 recolour source and matches no commit. Per CLEAN's proof-of-recovery gate neither is regenerable by a named command -> not deletable without confirmation. User: delete both, or keep style.css.bak as a deliberate manual backup? | verify: user decision recorded; stale backups removed or intentionally kept -- DONE: both files already absent from the tree (Test-Path False, zero *.bak* anywhere); untracked+gitignored so removal predates git; delete-or-keep moot. E-459
- [x] T-198 P1 full-suite stdout crash, T-179 family (HUNT 08.08.26). Two reproducible crashes: (a) INTERNALERROR `OSError: [Errno 9] Bad file descriptor` on pytest's terminal flush at ~88% of the suite, consistently right after test_watcher::test_two_simultaneous_writes_do_not_corrupt_cache (passes in isolation); (b) fatal exit code -1073740771 (0xC000041D) at interpreter shutdown even on a fully green run (813 passed). Something closes stdout fd mid-suite -- the T-179 stdout-None guards did not close this. | verify: 3 consecutive full-suite runs exit 0 with no INTERNALERROR and no -1073740771; the two_simultaneous cache test passes under full-suite load -- DONE: root cause = TestHotkeyListener lifecycle tests called REAL keyboard.add_hotkey, leaving a global Windows hook thread alive for the whole pytest process (Thread-1/Thread-2 survive stop()+unhook_all()); live hook callback at interpreter shutdown raises 0xC000041D, and the same leak under pytest capture gives the INTERNALERROR Bad-fd. Fixed with _NoOpKeyboard shim (tests never install a global hook; TestListenerRegistration already mocked the same contract). test_hotkey 19/19. 3 consecutive full-suite runs: 832 passed, 6 known pre-existing fails (4 protocol_sync foreign BASELINE, test_own_saipen_memory T-195 + validate T-185/T-150, test_grandfathered T-199), NO INTERNALERROR, NO 0xC000041D. Watcher clean in isolation. Shipped v0.1.25 (612e261, tag pushed, wheel PASS). E-453..E-456
- [x] T-201 README public-product/employer pass (user instruction, E-449). Top positioning 'Desktop tray viewer' -> 'Local Windows control center for SAIPEN projects'; new Why SAIPENVIEW / At a glance / Engineering evidence / Safety boundaries sections; conformance baseline honesty preserved + canonical SAIPEN repo linked; PyPI-not-published truth in Quick Start; absolute claims audited (removed FALSE 'auto-installs WebView2 on Win10', softened 'crash can never truncate', reworded 'no background IPC' -- guard.py loopback SHOW handoff is IPC, fixed phase diagram order BUILD->VERIFY->REVIEW, subSaipen list made MANIFEST-driven, outbox statuses +stale, api count 85->77, engines generic_cli->generic-cli); 33 i18n READMEs got a lag-notice; screenshots/dashboard-wide.png added repo-owned from user clipboard edit; GitHub About updated. Committed 8edaec5/4a3d3c8/7cbcbdf (merge of user remote web-edits 62e78cc/f80a751), pushed, remote README render + images + About verified. | verify: validate.py PASS (conformant); 243-test subset green (4 pre-existing protocol_sync fails = canonical 7.221 vs viewer baseline, documented); GitHub markdown render OK; local links/images none missing; i18n table/file parity 33/33
- [x] T-200 README rewrite (user instruction, E-446, commit 1501ff0). Full redo: giant i18n language bar collapsed into a <details> Translations table at the bottom; 2-col Features table to plain h3 sections; Quick Start to sequential code block; screenshots/dashboard.png promoted to hero. No code touched, no version bump. | verify: all 33 locale links preserved (rg file/link parity); README internal links resolve; no code files changed in the commit
- [x] T-197 P1 release-gate test is red at every shipped HEAD (HUNT 08.08.26). tools/release_gate.py correctly refuses `version 0.1.22 equals the newest tag -- re-shipping an old release`, but tests/test_release_gate.py::test_release_gate_passes asserts exit 0 unconditionally -- so the moment a release ships (version == newest tag), the gate refuses and the test goes red until the next version bump. The test is state-dependent: it can only pass in the window between bump and ship. Fix: make the test version-agnostic (run the gate against a sandboxed bumped version, or assert pass only when version > newest tag and skip/assert-fail at the shipped state). | verify: test_release_gate_passes green at a shipped HEAD (version == newest tag); gate still fails loudly when version < tag or four identity surfaces disagree -- DONE: release_gate.py gains --root override; test_release_gate_passes runs the gate against a sandboxed bumped-version tree (green at every HEAD, reproduced the red window in a sandbox git repo first); sandboxed fail-mode tests pin version-behind-newest-tag and surface-mismatch exits. 6/6 green, ruff clean, committed e8231ea. E-447..E-448
- [x] T-194 P2 refresh saiwiki WIKI-012 against current HEAD (mission N). The ready WIKI-012 payload (built @613e063, v0.1.10) is stale: README counts (api 66 -> 85 public methods, app.js ~3300 -> ~4286 lines) were already corrected by T-181, and the collect freshness gate (T-191) now refuses stale source_head anyway. Re-run `qq` (prepare saiwiki) at current HEAD, supersede the stale ready payload, generate a fresh one, then collect only after the freshness check passes. | verify: fresh WIKI payload source_head == current HEAD; stale ready entry marked superseded; collect passes the freshness gate -- DONE: qq prepared fresh WIKI-013 @71ed3a5 (bound to git-delta-v1:13a9c661 + role_revision 54a42475), qqq collected it, payload applied (README/CONTRIBUTING engines +opencode, tree +sessions/themes/paths/protocol_write, api 66->85, snap ctrl+q), 813 passed, shipped v0.1.23 (5b9d63a, tag pushed, wheel verify PASS). WIKI-012 stale payload superseded; OUTBOX WIKI-013 reviewed. E-442..E-445
- [x] T-192 Conformance rows rendered one character per line (user screenshot). The message was not the culprit: `.conf-where` holds a path (`.saipen/extensions/subs/saitranslate/kitchen/OUTBOX.md`) and was `flex: 0 0 auto`, so it claimed max-content width and refused to give any back; `.conf-msg` was the only flexible thing left and absorbed the whole shortfall -- `min-width: 0` on the message could not help, being willing to shrink is what killed it. Root cause of WHY it showed on a maximised window is a gap in T-155: `@container app` asks `body`, and a 1920px window routinely holds a 400px pane, so every app-band rule read 'plenty of room' while the pane starved. `.detail-pane` is its own query container now (`@container pane`, metadata drops to its own line below 640px), row metadata can always shrink, and message columns have a floor in `ch` not px. Applied to conformance rows, scan-error rows, ticket descriptions and outbox summaries -- same shape, same failure available to all of them. | verify: real WebView2 with the screenshot's exact row -- pane 291px: .conf-msg 0px/76 lines for 79 chars BEFORE, 275px/2 lines after; pane 588px: 58px/9 lines before, 572px/1 line after. tests/test_css_layout.py 22 cases (7 new), 7/7 red on the pre-fix stylesheet. 815 passed, ruff + format + node clean, commit 71ed3a5 -- E-436, E-437, E-438
- [x] T-183 P0 per-project write coordinator. ONE write coordinator used by EVERY .saipen mutator (update_state, move_ticket, reorder_ticket, record_manual_work, collect_outbox_entry, write_file_text on protocol files, add_human_note, future writers). Per-root lock + optimistic fingerprint/CAS: an external change between read and write aborts as a controlled conflict, never a silent lost update. Centralized E/T allocation (one allocator, no independent max+1 writers). Refuse direct protocol mutation while a Core agent runs for that root (ProcessManager.is_running). | verify: concurrency tests -- two simultaneous app writes allocate distinct IDs; external modification mid-mutation -> conflict, no lost update; mutation while agent running -> refused deterministically; concurrent collect/manual/toggle keep BOARD/LOG valid -- DONE: test_write_coordinator 13/13 (distinct ids under concurrency, CAS conflict, agent refusal, stale-tail fork blocked, origin attribution); full suite 809 passed green+deterministic | owner: opencode | claim_time: 2026-08-07T15:30:00Z
- [x] T-184 P0 LOG split-brain repair. Two E-356/E-357 branches exist: canonical active chain (E-356/E-357 at LOG:366/367, opencode) + stale claude-opus branch (LOG:428/429, 09:10/09:25, reusing the IDs). Quarantine the stale branch verbatim into .saipen/recovery/, splice marker + canonical DEC in active LOG (E-426), fix STATE.last_event to match active tail, no renumbering of canonical IDs. Regression: a stale/recovered writer cannot append when the observed tail changed since its read. | verify: validate.py shows 0 duplicate-E FAILs; quarantine file byte-holds the stale branch; STATE.last_event == active LOG tail -- DONE: stale claude-opus E-356/357 branch quarantined verbatim in .saipen/recovery/LOG-split-brain-E356-E357.md; active graph spliced + DEC E-427; validate.py 0 duplicate-E FAIL; regression test_stale_reader_cannot_append_to_a_moved_tail green
- [x] T-185 P0 BOARD split-brain repair. T-176/T-177/T-178 exist twice: canonical DONE copies (BOARD.md:9-11) + stale markhunt BLOCKED copies (BOARD.md:123-125). Keep canonical DONE; move stale BLOCKED copies verbatim to .saipen/recovery/ (the T-176 evaluate_js finding is real and gets re-ticketed fresh after verification). No ticket ID renumbering. Evidence: .saipen/recovery/BOARD-stale-markhunt-T176-178.md (verbatim quarantine). | verify: no duplicate T-ids on BOARD; validate.py duplicate-ID FAIL clears; recovery file holds the stale copies -- DONE: stale markhunt BLOCKED copies quarantined verbatim in .saipen/recovery/BOARD-stale-markhunt-T176-178.md; validate.py PASS no duplicate tickets; the real T-176 evaluate_js finding re-ticketed into T-190 and fixed
- [x] T-186 P0 STATE legality. STATE said phase DONE / transition_from HUNT with T-179 open -- HUNT->DONE is illegal. Repair: T-179 already claimed into DOING; move through the legal edge (SCOUT -> BUILD/VERIFY/REVIEW/SHIP/DONE). STATE/BOARD/LOG must describe the same work at every checkpoint. BAN synthetic user-brake: agent uncertainty is never a user brake; only real gates (destructive op, first publish, safety, explicit user brake) WAIT. | verify: validate.py no illegal-phase-edge FAIL; STATE.last_event == LOG tail; no WAIT present except at a real gate -- DONE: STATE repaired (execution_intent: normal, last_event == LOG tail); illegal HUNT->DONE gone; synthetic-brake ban documented in KNOWLEDGE/architecture.md
- [x] T-187 P1 own-conformance explained green. Fix every current FAIL with an explicit legacy policy, not checker-silencing: T-129 `| critical: true` closed-field violation (encode as description, don't widen grammar); ~40 DONE tickets without verify (documented grandfathered marker); LOG:43/60 markhunt without `tickets=`; legacy skeleton lines LOG:138-144/163-167 (documented mechanical migration preserving IDs/text/meaning, incl. the E-131 duplicate); saiwiki sub STATE (invalid next_action, str last_event, mid-line end); TEMPLATE sub STATE placeholders. Legacy policy documented + bounded + testable. | verify: validate.py 0 unexplained FAIL; each fix has a documented policy reference; regression tests for the recovery/merge duplicate-ID rule -- DONE: validate.py -> PASS: Agent is conformant (0 FAIL / explained WARN); docs/conformance-legacy.md explicit policy; test_conformance_legacy 5/5 incl. rules still fire on NEW violations
- [x] T-188 P0 release/version truth. Version files lagged v0.1.18..20 (tags shipped with pyproject/__init__ 0.1.17; v0.1.18 tag never existed; wheel METADATA lied). Make version ONE source (pyproject dynamic from saipenview.__version__), release gate asserting package==wheel METADATA==git tag==CHANGELOG heading; verify_wheel checks TAG identity not self-consistency; export_source.py derives archive name/version + records source HEAD from the SAME archived HEAD, fails on mismatch; CONTRIBUTING release instructions fixed; CHANGELOG 0.1.18/19/20 historical defect note. | verify: release gate test fails when any of the four identity values disagree; verify_wheel run on a tag mismatches loudly; export manifest records source HEAD -- DONE: single version source (pyproject dynamic from saipenview.__version__); tools/release_gate.py PASS; verify_wheel checks TAG identity; export_source derives version/name from archived HEAD + records source HEAD/version in MANIFEST; CONTRIBUTING release steps + CHANGELOG defect note
- [x] T-189 P1 dependency contract parity. pyproject vs requirements.txt disagree (watchdog >=3.0 vs >=4.0, psutil >=5.9 vs >=5.9.0, Nuitka build-dep placement). One authoritative contract: runtime deps in pyproject, build/dev in an extra; parity test so two install paths cannot silently install different floors. | verify: parity test passes; both files declare identical runtime floors -- DONE: test_dependency_parity 2/2; floors aligned (watchdog>=4.0, psutil>=5.9.0); nuitka moved to pyproject dev extra
- [x] T-182 Release process ships tags without bumping the committed version files. v0.1.18..v0.1.20 were tagged and pushed while pyproject.toml + saipenview/__init__.py stayed at 0.1.17 (the v0.1.20 wheel therefore carries METADATA 0.1.17; tools/verify_wheel.py triple-matches whatever the tree says, so its "0.1.20 PASS" recorded a pass at the wrong version). Ship step 2/3 never ran for those releases. Decide: make the release flow bump+commit pyproject/__init__ (and update verify_wheel to fail when the declared version is behind the newest tag), or document a deliberate single-source-of-truth alternative. | verify: two consecutive releases each bump pyproject + __init__ in the release commit; verify_wheel fails if the built version trails the latest tag -- DONE: superseded by T-188 (single version source + tools/release_gate.py + tag-identity verify_wheel)
- [x] T-190 P1 self-write attribution backend. Frontend selfWriteRoots as a boolean Set is an unsafe causal model: some actions mark the root BEFORE the write succeeds, a failed write leaves the marker armed so the next REAL external edit is suppressed, one Set token consumes only one watcher notification, and the global fileChangeDebounce can collapse events across roots. Move origin tracking backend-side: writer registers exact expected changed (root,file); watcher compares/consumes per (root,file) fingerprint; the pushed event carries origin=self/external/unknown; mark suppression only after a successful mutation; debounce per (root,file). ALSO: the watcher push has been dead in production -- api._window.evaluate_js called a MainWindow method that never existed (the quarantined stale MARKHUNT T-176 finding); the MainWindow.evaluate_js delegate is THIS ticket's prerequisite and a test must pin it exists. | verify: failed self-write followed by external edit -> external prompt appears; self-write touching BOARD+LOG -> no false prompt; simultaneous changes in two projects -> neither lost; multiple watcher events for one atomic replace -> one semantic notification; MainWindow.evaluate_js delegate present -- DONE: backend SelfWriteRegistry (post-write per-(root,file) fingerprints); Api pushes origin=self|external; frontend consumes origin; conftest no-subscriber-leak assertion; MainWindow.evaluate_js delegate; REAL bug found+fixed: app.run() early-return leaked the Api handler (the full-suite flake root)
- [x] T-191 P1 mutation idempotency + grammar (record_manual_work + collect_outbox_entry). Both were two/three independent commits with independent ID allocation and no rollback: record_manual_work could leave an unlogged orphan ticket (crash between BOARD and LOG), collect_outbox_entry could duplicate on retry after any partial step, and both let external text break the closed BOARD field grammar. Under the write coordinator: record is LOG-first with idempotent resume (prior LOG line -> same ticket id or no-op); collect is idempotent per (sub_name, entry_id) -- already-reviewed is a no-op, existing ticket/inbox/log line skips that step, OUTBOX marked reviewed LAST; pipes in descriptions/titles/summaries escaped via the shared helper; collect freshness-checks source_head against current HEAD. | verify: tests -- pipes/field-looking text escaped; injected failure between BOARD/LOG resumes without duplication; collect re-run after any partial step never duplicates; stale source_head refused -- DONE: test_protocol_idempotency 10/10 (pipes escaped, LOG-first resume with same ticket id, collect idempotent per (sub,entry) step incl. partial-state resume, stale source_head refused); test_parser green
- [x] T-181 Stale show/hide-hotkeys doc + settings feature-table truth. (a) index.html:92-93 says "3 bindings supported" and lists ctrl+q in the settings placeholder, but config.py:15-19 documents "exactly two slots" and DEFAULTS["hotkeys"] ships two (ctrl+alt+x, alt+f15); locale-en.js:75 and every other locale say "2 bindings supported". Decide the real slot count (config imposes no hard cap) and make all four places agree. (b) README.md:165 Settings feature row omits the colour-theme picker that has been in the modal since 0.1.12 (T-157) while README.md:241/262 document it (re-ticketed fresh from the quarantined stale MARKHUNT T-178 finding). | verify: label/placeholder/locale strings and the config comment agree; README feature row lists the theme picker; full suite + ruff + node --check clean -- DONE: index.html show/hide 2 bindings + placeholder fixed; README Settings row lists theme picker; README architecture counts corrected from a fresh scan (85 public methods, ~4.3k app.js lines)
- [x] T-179 Flaky shell test under load (mission P1, deterministic fix -- NOT a user decision). tests/test_generic_cli.py::TestShellContract::test_quoted_path_with_spaces (`if exist "C:\Program Files" echo PF_OK`) failed once under full-suite load and the same family struck 3+ more times: a scanner git-status subprocess reader dies with "Bad file descriptor" under load, crashing pytest's terminal flush. Root cause: parser.py get_git_status + _manual_work_git_note call `subprocess.run(...).stdout.strip()` unguarded -- a failed/closed reader thread yields stdout=None -> AttributeError on the scan thread (parser.py:1084/1092/419). NEVER retry an arbitrary already-spawned shell command (side effects, non-idempotent). Fix: (1) replace the environment-dependent "C:\Program Files" assertion with a deterministic temp-dir/file-with-spaces fixture; (2) guard `.stdout.strip()` -> `(...).stdout.strip() if stdout else ""` (or .communicate) so a failed reader never raises on a worker thread; (3) stress the deterministic fixture under suite load. Retry Popen ONLY for a narrowly-proven transient SPAWN failure, never after the child may have executed. | verify: new fixture is environment-independent (no C:\Program Files); 10x full-suite loop shows no reader-thread crash; stdout=None guard covered by a test; generic_cli suite green -- DONE: stdout-None guards in get_git_status/_manual_work_git_note/_current_head; deterministic temp-dir-with-spaces fixture replaces C:\Program Files; full suite deterministic green 809 passed
- [x] T-180 Default window-snap hotkey -> ctrl+q. User explicitly reverts the 4d291a0 / T-133 decision that freed ctrl+q from the defaults (its documented reason: a GLOBAL binding hijacks the combo in every app and ctrl+q is a ubiquitous quit accelerator -- the user accepts that tradeoff). Change DEFAULTS["snap_hotkey"] ["alt+f14"] -> ["ctrl+q"]; the load-time _is_ctrl_q strip (config.py:115-132) is obsolete once ctrl+q IS the default and would fight a saved ["ctrl+q"] -- remove it and its strip tests in tests/test_config.py (keep the string->list migration and the no-empty-slot fallback). Frontend/doc fallbacks must follow: app.js:2760 "alt+f14" -> "ctrl+q" (rewrite the 2757-2759 comment), index.html:96 placeholder, settings.snapHotkey.placeholder in all 34 locale-*.js, README.md:238 config example + every docs/i18n/README.*.md "Alt+F14" snap mention, test_hotkey.py:89 stale "shipped snap default" comment. index.html:273/281 help already documents Ctrl+Q -> stays. | verify: test_config.py updated (saved ["ctrl+q"] survives load unchanged, DEFAULTS["snap_hotkey"] == ["ctrl+q"], string->list still migrates); grep for alt+f14 no longer hits any snap-hotkey reference in app.js / index.html / locale-*.js / README.md / tests (only the show-hide "alt+f15" slot and test_hotkey's combo-mapping case remain); full suite 776 passed + ruff + node --check clean; RED CONTROL proven (3 new tests fail against HEAD config.py); shipped v0.1.21 (f5b9484, tag pushed, wheel triple-match PASS) | owner: opencode | claim_time: 2026-08-07T14:31:30Z

_(T-001..T-016 pruned by CLEAN 24.07.26 -- older than the active work above; full detail permanently in LOG.md + sealed .saipen/logs/LOG-001.md, nothing lost.)_

_(T-023..T-127 pruned by CLEAN 07.08.26 -- superseded by the active work above; full detail permanently in LOG.md + sealed .saipen/logs/LOG-001.md, nothing lost. Pruned: 111 tickets.)_

## BLOCKED
- [ ] T-529 P2 producer-package debt cluster: (a) .saipen/extensions/subs/saiwiki/kitchen/OUTBOX.md nonempty but parses as zero entries; (b) .saipen/saitranslate/kitchen/OUTBOX.md nonempty but parses as zero entries -- both FAIL under --gate collect:<producer> and block nothing today only because --gate core does not consume them; (c) wiki-mirror-stale + wiki-mirror-behind: saiwiki Scenarios page mirrors CONFORMANCE rows 1-254 at digest 687cb0c426e91d58, canonical now 6d92bc6244aee9be, behind by row 255 | verify: both OUTBOX files parse as valid packages or are emptied by their producers; Scenarios page re-mirrored to row 255 | blocker: hard-split producer deliverable owned by the dedicated saitranslate/saiwiki subSaipen instances; cleared by running ee (saipen prepare) -- Core agent must not route into it (CORE.md §1.2 instance-owned rule, E-496 precedent)
- [ ] T-195 P2 saitranslate bulk: translate the new README prose into the 30 non-Core locales. English README.md gained since 613e063: the `theme` palette section, the `Path safety & dry-run` section, the agent-engine bullets (generic-cli shell command, engine_overrides) and settings-modal row additions (snap hotkey, theme picker). ru/ee/ded are done (Core-owned, T-181-era). The 30 remaining mirrors (in .saipen/saitranslate/kitchen/) are synced for Ctrl+Q + counts but lack this prose; once translated, recompute each locale's source-digest from README.md (versions normalised to VERSION) and flip the OUTBOX to `status: ready`. NOTE (E-449): the public-product README pass rewrote the English again (control-center positioning, Why/At-a-glance/Engineering-evidence/Safety-boundaries) and added a lag-notice to every i18n README -- every mirror's digest is stale again, and the notice line itself must survive the retranslation | verify: all 33 mirrors carry the theme + path-safety + agent-engine content; every locale's source-digest matches README.md @ HEAD; OUTBOX status: ready; node --check + full suite green | blocker: hard-split producer deliverable owned by the dedicated saitranslate subSaipen instance; cleared by running ee (saipen prepare saitranslate) -- Core agent must not route into it (CORE.md §1.2 instance-owned rule, E-496)
- [ ] T-150 [FREEZE] GATE: no further multi-agent / agent-IDE surface until the SAIPEN protocol itself is sound. Reason is the protocol repo's own board, `V:\___VAC\__K\__CODE\_AI_STUFF_AGENTIC\_SAIPEN\.saipen\BOARD.md`: its T-442 [P0] forbids starting Concurrent Mode while the sequential protocol still has known foundational defects, and T-443..T-451 (the whole v8 Concurrent Mode contract -- proposals, atomic claims, wait-for graphs, release captain, reducer decision, soak) are all still open behind it. SAIPENVIEW's multi-agent surface is the GUI half of a contract that does not exist yet; building it now would hard-code a protocol that is still moving, and would be exactly the scope-widening T-442 refuses. Frozen at the user's instruction -- E-195, E-196. Frozen behind this gate: T-148 (multi-agent workspace), T-149 (protocol pane). NOT frozen: T-146/T-147 stay DONE and shipped in v0.1.9 -- single-agent correctness (adapters that never ran headless, transcripts that died with the process), no Concurrent Mode assumption anywhere in them. T-151 and the rest of TODO are ordinary work and continue | blocker: SAIPEN T-442 gate is open; unfreeze only when the user confirms the core protocol is clean (their words: ~99%). Unfreezing MUST name which of T-442's defect classes are closed, what green evidence backs it, and which sequential release was the stability checkpoint -- the same bar T-442 sets for itself
- [ ] T-148 One agent at a time is a viewer's constraint, not an IDE's. `renderAgentPanel` binds to a single `currentAgentPanelRoot` and the detail pane shows exactly one project, so two running agents cannot both be worked -- `list_running_agents()` and the fleet dashboard can only *report* them. Needs a way to switch between running agents (or see them side by side) without losing the output pane's scroll/state for the one you left | verify: two agents running in different projects, both reachable and both retaining their own output, without a full re-render on every switch | blocker: T-150 freeze
- [ ] T-149 The rules the agent is bound by are invisible. BOARD/STATE/LOG have viewers; RFC.md, STYLE.md and `phases/*.md` -- read from the project's own `saipen_home` -- have none, so the one document that decides every dispute cannot be opened from the tool built around it. Add a protocol pane reading the resolved `saipen_home`, degrading honestly when that path is dead (RFC § 1.7's clone instruction, not a blank panel) | verify: RFC and the active phase doc open from the detail pane; a bogus saipen_home says so instead of rendering empty | blocker: T-150 freeze
_(T-135 resolved 02.08.26 -- see T-142/E-170; the "generator" was Wintage's installer recolouring from a stale style.css.bak.)_
_(T-134 moved to DONE 02.08.26 -- see E-157.)_
_(previously: T-081/T-082 fixed, T-083 triaged to TODO, T-084 dismissed as a stale duplicate of the already-fixed T-093: textio.py's UTF-16 handling is deliberate defense against external tools writing UTF-16 -- see 29.07.26 LOG. All four resolved 30.07.26.)_

# saipenview — Audit Handoff

RUN_ID: acb-mtk1o0mv-702571e1c43a4f0392a7
GENERATED_AT: 2026-09-02T16:03:09.886086
PROJECT_NAME: saipenview
TARGET: saipenview_02.09.26-T10-14-53.zip -> extracted project root /
BASELINE: supplied archive SHA256 5e94adce8ca79092209b3492af8ccbd06481cc796fdd91f755bfa085007fb8bb; fix/audit-handoff@3164f6134f5213626d9943c512ad5e714c9a4959 plus supplied dirty working tree
GIT_CONTEXT: PRESENT fix/audit-handoff@3164f6134f5213626d9943c512ad5e714c9a4959 - supplied working tree is dirty (91 porcelain entries; implementation/tests modified plus bundled .venv_linux churn and untracked tests/test_core003_revision_ack.py)
SAIPEN_CONTEXT: PRESENT - STATE phase DONE/task none/next_action PHASE HUNT, last_event E-838; BOARD has only deferred T-50 [P1][PERF-007]; LOG records prior T-700..T-711 verification and final cleanup
TOTAL_TICKETS: 12
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 4 tickets (baseline: supplied archive SHA256 5e94adce8ca79092209b3492af8ccbd06481cc796fdd91f755bfa085007fb8bb; fix/audit-handoff@3164f6134f5213626d9943c512ad5e714c9a4959 plus supplied dirty working tree)
- Wave 2 (Second Wave): 4 tickets (baseline: supplied archive SHA256 5e94adce8ca79092209b3492af8ccbd06481cc796fdd91f755bfa085007fb8bb; fix/audit-handoff@3164f6134f5213626d9943c512ad5e714c9a4959 plus supplied dirty working tree)
- Wave 3 (Performance): 4 tickets (baseline: supplied archive SHA256 5e94adce8ca79092209b3492af8ccbd06481cc796fdd91f755bfa085007fb8bb; fix/audit-handoff@3164f6134f5213626d9943c512ad5e714c9a4959 plus supplied dirty working tree)

---
## 01 — AUDIT CORE
PROJECT_NAME: saipenview
DATE_TIME: 2026-09-02T15:00:00+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtk1o0mv-702571e1c43a4f0392a7
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
WAVE_ID: core
WAVE_INDEX: 1
WAVE_COUNT: 3
WAVE: AUDIT CORE
TARGET: saipenview_02.09.26-T10-14-53.zip -> extracted project root /
BASELINE: supplied archive SHA256 5e94adce8ca79092209b3492af8ccbd06481cc796fdd91f755bfa085007fb8bb; fix/audit-handoff@3164f6134f5213626d9943c512ad5e714c9a4959 plus supplied dirty working tree
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT fix/audit-handoff@3164f6134f5213626d9943c512ad5e714c9a4959 - supplied working tree is dirty (91 porcelain entries; implementation/tests modified plus bundled .venv_linux churn and untracked tests/test_core003_revision_ack.py)
SAIPEN_CONTEXT: PRESENT - STATE phase DONE/task none/next_action PHASE HUNT, last_event E-838; BOARD has only deferred T-50 [P1][PERF-007]; LOG records prior T-700..T-711 verification and final cleanup
AUDIT_SCOPE: entry points -> config/path normalization -> scan/discovery -> registry revision/reconciliation -> watcher/event bus -> parsing/conformance -> protocol writer/ownership -> external-change persistence/boundary gate -> process/session lifecycle -> service bridge/UI revision flow -> cache persistence -> focused regression tests
TEST_STATUS: TEST_PARTIAL
TEST_LIMITATION: Full suite could not be executed faithfully in this Linux sandbox. `python -m pytest -q --collect-only` collected 896 tests but ended with 21 collection errors because the host interpreter lacks runtime dependencies including watchdog, keyboard and webview; Windows-specific surfaces additionally depend on APIs such as os.startfile. Adding bundled `.venv_linux/Lib/site-packages` supplies watchdog for backend-focused tests but not the complete Windows runtime.
VERIFIED_INSTEAD: `python -m compileall -q saipenview` PASS; `PYTHONPATH="$PWD/.venv_linux/Lib/site-packages:$PWD" python -m pytest -q tests/test_audit_all3.py -k 'not w2_003_canonical_paths_only_persisted and not round2_t700_truncated_log_exact_zero_old_failures'` -> 43 passed, 2 deselected; tests/test_core003_revision_ack.py -> 1 passed; focused external-registry/startup tests -> 5 passed; deterministic local reproductions executed for CORE-001, CORE-002 and CORE-003; CORE-004 reproduced directly with its pytest case.
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: .saipen/{STATE.md,BOARD.md,LOG.md,KNOWLEDGE/architecture.md}; git HEAD/status/log/diff; pyproject.toml; requirements.txt; saipenview/{__main__.py,app.py,api.py,config.py,paths.py,scanner.py,watcher.py,events.py,parser.py,conformance.py,protocol_write.py,saio.py,ownership.py,external_changes.py,runtime.py,sessions.py,service.py}; saipenview/ui/static/{app.js,sai-api.js}; tests including test_audit_all3.py, test_api.py, test_core002_proven_death.py, test_core003_revision_ack.py and directly relevant lifecycle/cache/scanner/watcher coverage
COVERAGE_DEFERRED: Full Windows desktop execution (pywebview/window geometry/hotkeys/tray/native shell), complete 896-test runtime suite, Windows-only process/job-object behavior, and non-core performance surfaces reserved for later waves
CROSS_WAVE_REFERENCES: NONE - wave 1/3; .saipen contains historical audit work but no prior wave exists in this campaign run
RESIDUAL_UNCERTAINTY: Windows-only behavior was statically inspected but not executed; the supplied baseline contains extensive uncommitted changes, so findings apply to the exact archive working tree rather than clean HEAD alone

[P1] [CORE-001] saipenview/api.py:Api.refresh_known
EVIDENCE:
- api.py:1066-1070 snapshots `_full_refresh_pending` and immediately sets it False before any project has been successfully reconciled.
- api.py:1107-1121 treats OSError/subprocess errors as transient and deliberately preserves the previous cached row.
- api.py:1076-1088 then permits the next request with `known_revision == _registry_rev` to return `projects: None`, because the pending reconciliation obligation was already discarded.
- The `had_transient_failure` flag is only used at api.py:1142-1147 to suppress forced changed_roots; it never restores `_full_refresh_pending`.
- The retry-exhaustion path at api.py:1156-1158 likewise returns without restoring the pending obligation.
- Deterministic reproduction with one cached project and a fault-injected first `load_project()` produced:
  `after_first_pending=False`
  `after_first_calls=1`
  `after_second_calls=1`
  `second_projects_is_none=True`
  Thus the second refresh did not retry the transiently failed startup reconciliation.
- Existing `test_round2_t710_startup_pending_still_reconciles` verifies only the successful startup case and therefore does not cover this failure transition.
DEFECT:
A transient read failure consumes the one-shot startup reconciliation flag even though reconciliation did not succeed. The stale durable-cache row can then remain authoritative indefinitely when no later watcher event or scan repairs it; this is especially visible with auto-scan disabled or when the affected root produces no new filesystem event. The implementation correctly preserves the row during the transient failure but incorrectly marks the reconciliation obligation complete.
REPAIR:
Do not clear `_full_refresh_pending` at refresh entry. Preserve it until the pending reconciliation has completed without transient read failures and has survived the registry-revision commit check. If any root has a transient failure, or all three revision-conflict attempts are exhausted, keep/restore the pending state so a subsequent poll retries. Preserve the current protection against dropping rows on transient I/O and against clobbering concurrent registry mutations.
VERIFY:
- Seed a cached row with `_full_refresh_pending=True`.
- Fault-inject `load_project()` to raise OSError on the first refresh and return a changed valid project on the second.
- Assert first call preserves the old row AND leaves reconciliation pending.
- Call `refresh_known()` again with the revision returned by the first call; assert `load_project()` is invoked again, the changed row is committed, and only then `_full_refresh_pending` becomes False.
- Add a retry-exhaustion case proving the pending obligation also survives three registry-revision conflicts.
- Re-run T-710 successful-startup and post-scan no-blanket-reparse tests to preserve the intended PERF behavior.

[P1] [CORE-002] saipenview/external_changes.py:ExternalChangeRegistry corrupt-load lifecycle
EVIDENCE:
- external_changes.py:145-150 makes any existing `.corrupt` marker set `_load_corrupt=True`, clears all entries, and immediately returns.
- external_changes.py:189-203 handles malformed persistence by renaming the original JSON to the `.corrupt` marker/evidence path and setting `_load_corrupt=True`.
- external_changes.py:210-219 makes `_save()` unconditionally return False whenever `_load_corrupt` is True.
- external_changes.py:262-272 makes `flush()` a no-op in the same state.
- `record()` at 288-299 can add new evidence to memory but its `_save()` must fail, returning -1.
- `acknowledge()` cannot transition the registry back to trusted state; successful persistence is impossible while `_load_corrupt` remains True.
- `_load_corrupt` is described at 334-336 as lasting "until an evidence-safe reconciliation", but no such reconciliation transition exists in the class or supported API.
- Collect boundaries in parser.py (`collect_outbox_entry` and `_collect_freshness_precheck`) refuse whenever `get_registry().is_degraded()` is true, so the sticky state directly disables collection.
- Deterministic corruption/restart reproduction produced:
  `degraded_after_load=True`
  `json_exists_after_load=False`
  `corrupt_exists_after_load=True`
  `record_result=-1`
  `pending_after_record=1`
  `degraded_after_record=True`
  `degraded_after_restart=True`
  `pending_after_restart=0`
- Therefore newly observed evidence during degradation is memory-only and disappears on restart, while the application remains permanently fail-closed.
DEFECT:
The fail-closed corruption handling has no supported recovery transition. Once a corrupt persistence file is detected, the process can never make a successful save, restart sees the marker and becomes degraded again, and collect remains permanently unusable unless someone manually manipulates internal persistence files. The safety decision to preserve corrupt evidence is correct; the missing explicit reconciliation/recovery path is the defect.
REPAIR:
Keep corruption fail-closed and keep the original corrupt artifact archived, but add one explicit evidence-safe recovery transition. It must require deliberate caller/user authorization, retain any new in-memory PendingChange records observed after corruption, atomically create a new valid registry generation, and clear the corrupt marker only after that durable commit succeeds. Surface degraded/corrupt status and recovery result through the backend boundary rather than requiring filesystem surgery. Never silently reinterpret malformed evidence as an empty trusted registry.
VERIFY:
- Start from malformed external_changes.json; assert collect is blocked and corrupt evidence remains preserved.
- Record a new external change while degraded.
- Invoke the explicit recovery action; assert the new pending evidence is present in the newly committed registry and the archived corrupt evidence remains available.
- Restart a fresh ExternalChangeRegistry; assert it is no longer degraded and the post-corruption pending record survives.
- Acknowledge that record and assert the acknowledgement persists across another restart.
- Fault-inject recovery persistence failure and prove the marker remains, the corrupt artifact is retained, and collection stays fail-closed.

[P1] [CORE-003] saipenview/api.py:Api.stop / saipenview.events:event_bus ownership
EVIDENCE:
- events.py:27-32 defines one module-level shared EventBus singleton.
- events.py:34-44 allows independent components to subscribe arbitrary callbacks; agent lifecycle events and service events share this bus.
- Api.start owns only its `saipen.project_changed` wrapper, registered at api.py:2566-2574.
- Api.stop correctly unsubscribes that owned callback at api.py:2616-2619, but then api.py:2620-2624 calls `event_bus.clear()`, deleting every subscriber owned by every other component.
- service.py:209 independently subscribes to `saipen.file_changed`; runtime/tests also use independent `agent.finished`, `agent.output`, and related subscriptions.
- Deterministic reproduction registered an unrelated `agent.finished` subscriber, then executed Api.stop:
  `before=True`
  `after=False`
  `delivered=0`
- The unrelated subscriber was destroyed even though Api never owned it.
DEFECT:
Component teardown is clearing a process-global event registry. Stopping one Api instance can silently sever unrelated event consumers, including another embedded component or another Api/service integration sharing the same EventBus. This violates subscription ownership and turns a local lifecycle action into global state destruction.
REPAIR:
Remove `event_bus.clear()` from `Api.stop()`. Api must unsubscribe only callbacks it registered, with owned callback references tracked explicitly. If process-wide EventBus clearing is desired for tests or final process shutdown, perform it in the actual process/test owner after all components are stopped, not inside Api lifecycle teardown. Preserve the existing generation wrapper and restart idempotence behavior.
VERIFY:
- Subscribe an unrelated callback before Api.start/stop; after Api.stop publish its event and assert it still fires.
- Assert the stopped Api's own `saipen.project_changed` wrapper no longer fires.
- Start -> stop -> start the same Api and assert exactly one fresh wrapper subscription exists.
- Run two independent Api/component subscribers, stop one, and prove the other continues receiving events.
- Re-run service lifecycle and stale-callback regression tests.

[P2] [CORE-004] tests/test_audit_all3.py:test_round2_t700_truncated_log_exact_zero_old_failures
EVIDENCE:
- test_audit_all3.py:653 explicitly says it seeds ">100 future-stamped entries".
- Lines 655-659 hard-code every supposedly future entry to `30.08.26 23:59`.
- Current audit date is 2026-09-02, so those timestamps are already historical.
- conformance.py:1548-1556 emits `log.timestamp.future` only when the parsed timestamp is more than LOG_CLOCK_SLACK_SECONDS ahead of the current UTC clock.
- Direct execution on 2026-09-02 failed at test_audit_all3.py:667:
  `assert conf.get("findings_truncated") is True`
  Actual report contained only one `state.last_event.stale` failure and no `findings_truncated`.
- The remaining audit_all3 subset passed when this case and the Linux-incompatible Windows-path case were excluded: `43 passed, 2 deselected`.
DEFECT:
The regression fixture encodes a calendar assumption that expired three days after it was written. The test now fails for reasons unrelated to T-700 and no longer verifies the >100-finding/truncation invariant. This creates a permanently red regression surface and can mask genuine conformance regressions behind a stale fixture failure.
REPAIR:
Make the clock relationship deterministic. Prefer freezing/monkeypatching the conformance clock for this test and constructing LOG timestamps relative to that fixed UTC instant; alternatively derive the test timestamp from current UTC plus more than LOG_CLOCK_SLACK_SECONDS. Do not replace it with another arbitrary future calendar date. Keep the second "clean LOG" phase deterministic against the same clock/state fixture.
VERIFY:
- Run the repaired test independently and confirm the first phase produces >100 `log.timestamp.future` findings with `findings_truncated=True`.
- Confirm the repaired LOG produces verdict pass, fails=0 and no stale truncation flag.
- Run the case under at least two simulated dates separated by years to prove calendar independence.
- Re-run the complete tests/test_audit_all3.py module in a supported Windows dependency environment.

CORE_DONE_WHEN: CORE-001 transient startup reconciliation retries until a successful authoritative commit; CORE-002 corrupt external-change evidence has an explicit fail-closed, durable recovery path with post-corruption evidence preserved; CORE-003 Api teardown removes only Api-owned EventBus callbacks; CORE-004 T-700 truncation coverage is clock-independent; compileall and focused regressions pass, then the full supported Windows test suite completes without CORE regressions.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: saipenview
DATE_TIME: 2026-09-02T15:40:22+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtk1o0mv-702571e1c43a4f0392a7
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
ACB_CHAIN_RECEIPT: second-mtk2hwp9-5a0ee893964e
WAVE_ID: second
WAVE_INDEX: 2
WAVE_COUNT: 3
WAVE: AUDIT SECOND WAVE
TARGET: saipenview_02.09.26-T10-14-53.zip -> extracted project root /mnt/data/audit_saipenview_w2
BASELINE: supplied archive SHA256 5e94adce8ca79092209b3492af8ccbd06481cc796fdd91f755bfa085007fb8bb; fix/audit-handoff@3164f6134f5213626d9943c512ad5e714c9a4959 plus supplied dirty working tree
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT fix/audit-handoff@3164f6134f5213626d9943c512ad5e714c9a4959 - supplied tree has 91 porcelain entries, including modified implementation/tests, bundled .venv_linux churn and untracked test/scaffold material
SAIPEN_CONTEXT: PRESENT - STATE phase DONE/task none/next_action PHASE HUNT/last_event E-838; BOARD contains only deferred T-50 [P1][PERF-007]; declared saipen_home is C:/Users/vac34/.agents/skills/saipen and is unavailable in this Linux sandbox
AUDIT_SCOPE: startup/shutdown/restart boundaries; concurrent canonical-engine loading; global module/cache authority; watcher topology lifecycle; in-flight refresh cancellation; external-change persistence deserialization and collect boundary; hotkey transactional rollback; scanner/service lifecycle; focused lifecycle/regression tests
TEST_STATUS: TEST_PARTIAL
TEST_LIMITATION: Project is Windows-only. Full intended suite cannot run faithfully in this Linux sandbox because runtime dependencies/native surfaces are missing or incompatible, including keyboard, webview and usable platform-native psutil components, and the STATE-declared Windows saipen_home is unreachable. A focused run containing hotkey-listener tests produced 11 passes plus 2 failures caused at import by `ModuleNotFoundError: No module named 'keyboard'`.
VERIFIED_INSTEAD: `python -m compileall -q saipenview` PASS; environment-independent focused regression set -> 11 passed in 0.61s; deterministic local concurrency/lifecycle reproductions verified W2-001 through W2-004 without modifying the audited tree
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: .saipen/{STATE.md,BOARD.md,LOG.md}; git HEAD/status/diff; README.md; pyproject.toml; requirements.txt; saipenview/{__main__.py,app.py,api.py,config.py,external_changes.py,guard.py,hotkey.py,parser.py,protocol_write.py,runtime.py,saio.py,scanner.py,service.py,sessions.py,watcher.py}; lifecycle/boundary tests including test_api.py, test_audit_all3.py, test_second_wave.py, test_w2_009_hotkey_rollback.py, test_w2_011_stale_callback.py, test_w2_013_toctou.py, test_w2_023_gen_token_race.py, test_w2_027_registry_persistence.py and test_watcher.py
COVERAGE_DEFERRED: Windows-native WebView2/window/hotkey/job-object execution; complete supported-Windows pytest matrix; performance-only scanner/cache/runtime surfaces reserved for wave 3
CROSS_WAVE_REFERENCES: CORE-001 startup reconciliation obligation loss remains separate; CORE-002 corrupt ExternalChangeRegistry has no recovery transition and W2-003 below additionally proves semantically malformed-but-parseable persistence can bypass corruption detection entirely; CORE-003 global EventBus clearing remains separate but compounds restart lifecycle risk; CORE-004 stale calendar fixture remains test-only
RESIDUAL_UNCERTAINTY: Native Windows scheduler/watchdog timing could expose additional interleavings not reproducible here; supplied baseline is heavily dirty, so all findings apply to the exact archive working tree rather than clean HEAD alone

[P0] [W2-001] saipenview/saio.py:engine / _load_codec_from / _load_freshness_from
EVIDENCE:
- `saio.py:42` defines process-global `_ENGINE_CACHE` with no synchronization.
- `engine()` resolves the project home, checks `_ENGINE_CACHE` at lines 163-184, mutates process-global `sys.path` at 186-188, imports the globally named `saipen_engine.*` modules at 189-199, and only publishes its home into `_ENGINE_CACHE` at line 202. That complete check -> sys.path mutation -> import -> publication transaction is unlocked.
- `_load_codec_from()` and `_load_freshness_from()` independently perform the same global-home guard and `sys.path` mutation without a shared lifecycle lock, so locking only `engine()` would still leave contamination paths.
- `service.py` uses `ThreadingHTTPServer`, so separate authenticated requests may execute backend work concurrently. The process therefore has a legitimate multi-threaded entry path into SAIO.
- Deterministic reproduction used two temporary canonical homes with real `saipen_engine` packages whose modules reported distinct origins. A barrier was inserted only around the first `import_module` call so both production `engine()` calls had already passed the unlocked cache guard. Result:
  `errors= {}`
  `cache_keys= [.../homea, .../homeb]`
  `A_operations_origin= B`
  `B_operations_origin= B`
  Thus both distinct homes were accepted even though the function explicitly promises exactly one home, and project A received project B's canonical engine module.
- A separate same-home reproduction exposed inconsistent cache identity: `_load_freshness_from()` stores `str(home)` while `engine()` stores `str(home).lower()`. With `MixedCaseHome`, freshness produced one cache key and `engine()` added a second lowercase key for the same home:
  `after_engine_keys=[.../MixedCaseHome, .../mixedcasehome]`
  `same_home_duplicate_entries=2`.
DEFECT:
The canonical writer authority is not initialized atomically. Two first-use requests for different SAIPEN homes can both pass the fail-closed multi-home guard, race through global `sys.path`/`sys.modules`, and permanently cache the same imported engine implementation under two different project-home identities. A subsequent mutation can therefore execute against the wrong canonical engine version. This defeats the module's stated one-home safety invariant and can produce protocol writes using authority from another project/home.
REPAIR:
Introduce one process-global re-entrant authority-loader lock and one canonical home-key function. Hold that lock across home compatibility check, cache lookup, `sys.path` preparation, all canonical imports, completeness validation and cache publication for `engine()`, `_load_codec_from()` and `_load_freshness_from()`. Use the identical normalized key in all three paths; on Windows it must collapse case/slash aliases consistently. Do not publish a partial cache entry. A competing distinct home must deterministically receive `SaioUnavailable` before its path can influence `sys.path` or module resolution. Preserve same-home concurrent reuse.
VERIFY:
- Repeat the two-home barrier reproduction with real temporary modules; exactly one home may initialize and the other thread must fail with `SaioUnavailable`.
- Assert `_ENGINE_CACHE` contains exactly one canonical home key and every returned module originates from that home.
- Run 20+ concurrent same-home calls across `engine`, codec and freshness loaders; all must succeed and share one authority/module identity.
- Exercise mixed-case/slash aliases for the same Windows path and assert one cache entry.
- Exercise two concurrent service requests targeting projects whose STATE files declare different homes and assert deterministic fail-closed behavior with zero wrong-home canonical calls or writes.

[P1] [W2-002] saipenview/api.py:Api._do_root_refresh/stop + saipenview/watcher.py:SaipenWatcher.sync/stop
EVIDENCE:
- `_do_root_refresh()` checks its lifecycle generation only once at `api.py:597-601`, before potentially expensive parsing/cache work.
- After that check it executes `_refresh_one_project()` at lines 614-617 and can push JavaScript at 618-633 without another generation check.
- `Api.stop()` increments `_stop_gen` first at 2588-2594 and cancels pending timers only later at 2604-2608. `Timer.cancel()` cannot stop a callback that is already executing.
- Deterministic reproduction blocked an already-entered `_refresh_one_project`, incremented `_stop_gen` exactly as `stop()` does, then released the old callback. The stale callback still performed its post-refresh window dispatch:
  `stop_gen=1`
  `callback_gen=0`
  `js_pushes_after_generation_invalidated=1`
  `changed_queue_remaining={}`.
- The same shutdown boundary reaches watcher topology. `SaipenWatcher.sync()` reads `_stopped` outside `_lock` at watcher.py:300 and, in scan-root mode, `_sync_by_scan_roots()` plus `_watch_scan_root()` mutate `_watches`, `_handlers`, `_root_router`, `_project_to_scope` and `_fallback_projects` without holding `_lock`.
- `SaipenWatcher.stop()` sets `_stopped=True` and clears those maps under `_lock` at 504-516, then stops the Observer.
- Deterministic schedule/stop interleaving: a sync thread was blocked inside `Observer.schedule`; `stop()` completed and reported zero watches/routers; allowing `schedule()` to return then resurrected state after shutdown:
  `after_stop_before_release={'stopped': True, 'watches': 0, 'routers': 0, 'observer_stopped': True}`
  `after_sync_returns={'stopped': True, 'watches': 1, 'handlers': 1, 'routers': 1, 'project_map': 1, 'observer_stopped': True}`.
- `_sync_watcher()` is reachable from cache publication, vanished-project refresh, refresh_known and start, so watcher sync is not intrinsically single-threaded.
DEFECT:
Shutdown is not a true lifecycle barrier. Work that passed the generation check immediately before stop can continue mutating cache/UI state after invalidation, and a concurrent watcher reconciliation can repopulate topology after `SaipenWatcher.stop()` cleared it. On a stop/start cycle, stale generation work can overlap newly revived state, causing stale refreshes, post-stop dispatch, ghost watch bookkeeping or duplicate/missing observation.
REPAIR:
Make lifecycle invalidation cover the entire in-flight refresh/topology transaction rather than only callback entry. Track active root-refresh work or pass the captured generation through the refresh commit path, rechecking immediately before every cache/UI/watcher side effect; `stop()` must invalidate pending generations and synchronize with callbacks that already entered before watcher teardown/restart. Independently make watcher topology reconciliation generation-aware and serialized: all topology maps must mutate under one lifecycle discipline, and a watch returned after its captured generation became stopped/stale must be immediately cancelled/unscheduled rather than committed. `stop()` and `revive()` must invalidate any in-flight sync generation.
VERIFY:
- Barrier an old `_do_root_refresh` after entry, call `stop()`, release it, and assert zero cache mutation, watcher synchronization and JS dispatch after the stop barrier.
- Repeat with stop -> start before releasing the old callback; old generation work must not modify the restarted instance.
- Barrier `Observer.schedule`, stop the watcher, release schedule and assert `_stopped=True` with all topology maps empty and no live scheduled watch.
- Race sync/stop/revive repeatedly with scan-root and fallback topology and assert maps correspond only to the newest lifecycle generation.
- Preserve W2-011 stale-callback, CORE-005 watcher-revive, T-124 watch lifecycle and normal debounce behavior.

[P1] [W2-003] saipenview/external_changes.py:ExternalChangeRegistry._load
EVIDENCE:
- The registry explicitly promises canonical root keys and normalized relative paths (`normalize_rel()` rejects absolute/traversal paths at lines 38-52; `record()` canonicalizes both at 288-290).
- `_load()` does not apply either invariant to persisted rows. At lines 168-179 it directly constructs `PendingChange` from persisted `root`, `path`, `fingerprint`, `observed_at`, `token` and `status`; only constructor-level KeyError/TypeError/ValueError is treated as corruption.
- `PendingChange` itself performs no validation, so syntactically valid JSON containing impossible semantic values remains trusted.
- `unresolved()` at 361-368 filters specifically on `status == "unresolved"`, while collect boundaries at parser.py:1299-1318 and 1590-1610 trust `is_degraded()` plus `unresolved(root)` as their safety evidence.
- Deterministic reproduction persisted a row for the correct canonical project/root and valid `STATE.md` path but used an impossible status `"garbage"`. Load result:
  `degraded=False`
  `pending_count=1`
  `unresolved_count=0`
  `loaded_status=garbage`
  The registry therefore considered the file trusted while its only pending evidence became invisible to the collect boundary.
- A second reproduction loaded a row containing noncanonical root `.../project/..`, traversal path `../STATE.md`, invalid fingerprint text, nonnumeric observed_at and status `acknowledged`. Result:
  `degraded=False`
  `load_untrusted=False`
  `unresolved_actual_root=[]`
  `corrupt_marker_exists=False`.
- Current writers remove acknowledged entries rather than persist them, and no current path creates arbitrary statuses; such rows are persistence corruption, not valid historical state.
DEFECT:
Corruption detection validates JSON shape but not the registry's own semantic invariants. A parseable but damaged persistence file can therefore be promoted to trusted state while pending external-change evidence is hidden from `unresolved()`. Because collect explicitly uses this registry as a fail-closed mutation boundary, semantic corruption can turn a required refusal into apparent clean state.
REPAIR:
Validate the complete persisted snapshot before committing any row to `_entries`. Require each root to equal its canonical spelling, each path to equal `normalize_rel(path)`, status to be a currently persistable state (`unresolved` for the present schema), token to be a valid positive integer, observed_at to have the expected numeric type, required string fields to have valid basic shape, and serialized `(root,path)` keys to be unique. Validate the generation counter against loaded tokens. If any row violates the persisted schema, reject the entire snapshot through `_mark_corrupt`; never partially load or silently normalize damaged evidence. Keep compatibility with fingerprint strings accepted by the current public `record()` contract unless that contract is deliberately versioned.
VERIFY:
- Persist valid rows and prove restart behavior remains unchanged.
- Persist each semantic violation independently: traversal path, noncanonical root alias, unknown status, bad token/type, duplicate `(root,path)`, inconsistent next_token. Every case must set load-untrusted/degraded and preserve the original corrupt artifact.
- Specifically rerun the `"garbage"` status reproduction and assert the registry cannot report `degraded=False`.
- At the collect boundary, prove a semantically malformed persistence file refuses with BOUNDARY_VIOLATION rather than appearing to have zero unresolved evidence.
- Preserve CORE-002's separate requirement for an explicit evidence-safe recovery path after corruption has correctly been detected.

[P2] [W2-004] saipenview/api.py:Api.set_hotkeys / saipenview/hotkey.py:HotkeyListener.set_hotkeys
EVIDENCE:
- `HotkeyListener.start(strict=True)` explicitly catches both `ValueError` and `ImportError` from hotkey registration at hotkey.py:205-224 and re-raises the first error after restoring listener state.
- `HotkeyListener.set_hotkeys()` at 245-256 catches any `Exception`, restores `_hotkeys`, then re-raises. Its callback contract therefore includes `ImportError`.
- `Api.set_hotkeys()` durably commits the new config before calling the runtime binding callback at api.py:2414-2419, but its rollback catches only `(ValueError, KeyError)` at 2420. An `ImportError` escapes before persisted/live config is reverted.
- The adjacent `set_snap_hotkey()` already uses the broader defensive exception contract, so the two equivalent setting paths have asymmetric rollback semantics.
- Deterministic reproduction used the real `_mutate_config` persistence transaction and a binding callback raising `ImportError("backend unavailable")`. Result:
  `raised=ImportError backend unavailable`
  `live_hotkeys=['alt+f15']`
  `disk_hotkeys=['alt+f15']`
  while the HotkeyListener contract would have retained its prior runtime binding.
- Existing API regression coverage exercises `ValueError`; the dedicated HotkeyListener tests cannot run in this sandbox because importing `keyboard` fails, but source inspection confirms ImportError is an intentional registration failure type.
DEFECT:
A supported hotkey-binding failure class bypasses the API rollback transaction. Runtime listener state can remain on the old bindings while in-memory and persisted config claim the new bindings, and the RPC/UI receives an uncaught exception instead of the documented `ok:false` setter result.
REPAIR:
Make `Api.set_hotkeys()` use the same binding-failure contract as the callback it invokes. At minimum include `ImportError`; preferably centralize the binding callback exception handling shared by primary and snap hotkeys so their transaction semantics cannot drift again. On any ordinary binding failure, restore the previous runtime binding, restore persisted/live config, and return `ok:false`. Do not catch BaseException classes such as KeyboardInterrupt/SystemExit.
VERIFY:
- Add API-level regression for callback `ImportError`; assert no exception escapes, result is `ok:false`, live config equals previous config and disk contains the previous hotkeys.
- Verify the callback is invoked with the previous binding during rollback.
- Preserve existing ValueError rollback and successful binding tests.
- In the supported Windows environment, run the HotkeyListener strict rollback suite and the API setter contract suite together.

SECOND_WAVE_DONE_WHEN: Canonical SAIO authority initialization is atomic and one-home under concurrent first use; stop/restart forms a real barrier against already-entered refresh and watcher-sync work; persisted external-change evidence is semantically validated and fails closed on any malformed row; all supported hotkey binding failures roll back runtime/live/disk state consistently; focused concurrency regressions pass and the complete Windows CI pytest matrix is green.

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: saipenview
DATE_TIME: 2026-09-02T15:54:06+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: acb-mtk1o0mv-702571e1c43a4f0392a7
CAMPAIGN_MANIFEST_SHA256: 97d9d053e37e6597a8559008911f84b28f799f326a97c69ebc5b304690defa72
ACB_CHAIN_RECEIPT: performance-mtk36y3j-dd858df7086c
WAVE_ID: performance
WAVE_INDEX: 3
WAVE_COUNT: 3
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: /mnt/data/saipenview_02.09.26-T10-14-53.zip -> extracted project root /mnt/data/audit_saipenview_perf
BASELINE: supplied archive SHA256 5e94adce8ca79092209b3492af8ccbd06481cc796fdd91f755bfa085007fb8bb; fix/audit-handoff@3164f6134f5213626d9943c512ad5e714c9a4959 plus supplied dirty working tree
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT fix/audit-handoff@3164f6134f5213626d9943c512ad5e714c9a4959 - 91 porcelain entries in supplied tree, including modified implementation/tests, bundled .venv_linux churn and untracked test material
SAIPEN_CONTEXT: STALE - STATE is DONE/PHASE HUNT at E-838, but BOARD still carries deferred T-50 [P1][PERF-007] terminal retention while LOG E-806/E-807 records the equivalent bounded-retention work as verified and shipped
AUDIT_SCOPE: scan executor capacity and timeout recovery; cache sidecar startup complexity/I/O; ticket-index construction, eviction and search lifecycle; repeated parser/serializer paths; agent-status polling and process metrics; bounded process/output/cache structures; frontend polling/DOM/event hot paths; focused performance regressions
TEST_STATUS: TEST_PARTIAL
TEST_LIMITATION: Windows-native UI verification is unavailable in this Linux sandbox. `PYTHONPATH="$PWD/.venv_linux/Lib/site-packages:$PWD" python -m pytest -q tests/test_perf001_log_incremental.py tests/test_perf003_render_split.py tests/test_perf005_live_dom.py tests/test_perf006_cancellation.py tests/test_perf007_tail_reads.py tests/test_perf008_output_framing.py tests/test_perf009_output_events.py` produced 61 passed and 1 environment failure: `ModuleNotFoundError: No module named 'webview'` while importing saipenview.ui.window for test_geometry_unchanged_skips_save.
VERIFIED_INSTEAD: `python -m compileall -q saipenview` PASS; 61 focused PERF tests PASS; deterministic executor-starvation reproduction; sidecar canonicalization-count and startup scaling measurements; duplicate BOARD-read reproduction; authoritative-removal ticket-index retention reproduction
STATUS: PERFORMANCE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: .saipen/{STATE.md,BOARD.md,LOG.md,KNOWLEDGE/architecture.md}; git context; pyproject.toml; requirements.txt; saipenview/{api.py,scanner.py,parser.py,runtime.py,sessions.py,watcher.py,service.py,conformance.py,external_changes.py,events.py,saio.py,protocol_write.py}; saipenview/ui/static/{app.js,sai-api.js}; focused PERF/scanner/API/runtime tests
COVERAGE_DEFERRED: actual Windows/WebView2 DOM and bridge timings; real disconnected-drive/kernel-I/O timing; native psutil cost under a 12-agent Windows fleet; complete supported-Windows pytest matrix
CROSS_WAVE_REFERENCES: CORE-001 startup reconciliation loss is separate from the startup cache-cost finding below; CORE-003 global EventBus teardown remains a lifecycle defect rather than a hot-path issue; W2-002 covers stop/revive synchronization whereas PERF-001 below is persistent executor-capacity starvation even without a stop race; W2-001 canonical-engine concurrency is separate
RESIDUAL_UNCERTAINTY: absolute Linux benchmark timings do not predict Windows timings, but the verified O(N^2) call count, duplicate reads, stale-index retention and executor starvation are platform-independent structural results; frontend process-metric cost was verified by call graph/cadence rather than native WebView2 profiling

[P1] [PERF-001] PROVEN BOTTLENECK saipenview/scanner.py:_get_shared_pool/scan timeout lifecycle
EVIDENCE:
- scanner.py:546-565 creates one process-global ThreadPoolExecutor and chooses its `max_workers` from the FIRST scan only: `min(max(n_workers, 1), _SHARED_POOL_MAX)`.
- scanner.py:568-595 retains that executor while `_SHARED_POOL_FUTURES != 0`; a timed-out running future therefore keeps both its worker and the entire original-capacity pool alive.
- scanner.py:628-638 submits every later scan to that same retained executor.
- scanner.py:645-678 times out the coordinator and cancels only futures that have not started. Python cannot cancel the already-running blocked worker.
- The stated scan invariant at scanner.py:606 is that one slow/hung drive must not starve the rest, but a legitimate one-root initial configuration creates a one-worker persistent pool.
- Deterministic reproduction replaced only `_scan_root_task`, set the existing timeout to 50 ms, and blocked the first root indefinitely:
  `first_complete=False hung_started=True elapsed=0.054`
  `pool_workers_after_timeout=1 futures=1`
- A second scan containing only healthy roots then reused the occupied one-worker pool:
  `second_complete=False healthy_ran=[] elapsed=0.050 unresolved=['healthy1', 'healthy2', 'healthy3']`
- No healthy second-cycle worker executed until the old hung root was manually released.
- Existing test_perf006_cancellation.py:132-154 verifies only that executor thread count remains bounded across timeout cycles. It does not verify that bounded capacity remains usable.
ISSUE:
The prior fix changed an unbounded abandoned-thread problem into persistent capacity poisoning. A single truly blocked root can monopolize the complete executor when the first configured scan contains one root; larger initial pools progressively lose usable workers as non-cooperative timed-out calls accumulate. The thread count is bounded, but useful scan capacity is not protected. Future healthy roots can therefore repeatedly time out without ever starting, making discovery effectively dead until the blocked OS call returns or the process restarts.
OPTIMIZE:
Treat timed-out running workers as quarantined capacity rather than healthy reusable capacity. Do not reuse an executor generation when abandoned non-cooperative work consumes all capacity required by a new scan. Preserve a hard global resource bound while providing fresh current-generation capacity; if the hard abandoned-worker budget is exhausted, expose an explicit degraded/no-capacity result instead of queueing healthy roots behind known-stuck work. Where possible, move potentially indefinite filesystem preflight/walk operations behind genuinely time-bounded or killable isolation rather than relying on ThreadPoolExecutor cancellation, which cannot terminate a running filesystem call.
GUARDRAIL:
- Never restore per-cycle unbounded thread creation.
- Preserve `_inflight_roots` duplicate-root quarantine.
- Preserve partial ScanOutcome semantics and unresolved-root protection.
- A timed-out old generation must never publish authoritative project results into a newer generation.
- Resource exhaustion must fail visibly/degraded, not falsely produce an authoritative empty scan.
VERIFY:
- Re-run the exact one-hung-root -> three-healthy-roots reproduction; healthy roots must execute during the second cycle without waiting for the hung call.
- Repeat with enough non-cooperative roots across multiple cycles to exercise the global abandoned-work bound; thread/process count must remain bounded.
- Assert no duplicate worker runs for the same in-flight canonical root.
- Assert a saturated quarantine budget returns explicit incomplete/degraded state.
- Preserve test_perf006_cancellation.py's bounded-worker and cancellation tests.

[P2] [PERF-002] PROVEN BOTTLENECK saipenview/api.py:Api._load_cache_records
EVIDENCE:
- api.py:1243-1275 reads and JSON-parses every live sidecar once during validation, but stores only its Path in `record_files`.
- api.py:1301-1308 then reads and JSON-parses every surviving file a second time before applying it.
- api.py:1309-1312 scans every root already accumulated in `by_root` for every live record and canonicalizes both sides of every comparison.
- New deleted markers at api.py:1253-1259 and legacy tombstones at 1285-1299 each rebuild the entire `by_root` dictionary while hashing/canonicalizing every root, adding another whole-registry pass per deletion state.
- Instrumenting `_canonical_or` with otherwise-valid sidecars produced exactly quadratic call counts:
  `n=10 canonical_calls=100`
  `n=20 canonical_calls=400`
  `n=40 canonical_calls=1600`
- A deterministic local startup benchmark with valid digest-bound sidecars produced:
  `n=50 elapsed=0.0496 loaded=50`
  `n=100 elapsed=0.1890 loaded=100`
  `n=200 elapsed=0.6745 loaded=200`
  `n=400 elapsed=2.6913 loaded=400`
- Doubling 200 -> 400 rows increased this isolated loader cost by approximately 4x, matching the proven N^2 canonicalization count. Absolute times are environment-specific; the complexity is not.
ISSUE:
The durable-cache optimization has an O(N^2) startup overlay path and performs two complete JSON reads of every valid sidecar. Cache state intended to accelerate startup therefore becomes increasingly expensive as project history grows. Deletion/tombstone-heavy stores add further repeated O(N) passes.
OPTIMIZE:
Parse each sidecar exactly once into a validated in-memory state keyed by its filename digest. Build one canonical-root/digest index for the legacy `_projects` snapshot, then apply live/deleted/legacy-tombstone precedence with O(1) digest lookups. Keep canonicalization/hash computation to O(N) total. Do not re-open valid record files in the application pass.
GUARDRAIL:
- Preserve `_valid_cache_row` validation.
- Preserve filename-digest binding checks.
- Preserve the current new-marker versus legacy `.deleted` mtime precedence.
- Preserve malformed-record quarantine and crash-atomic single-file deletion semantics.
- Do not let optimization turn an unreadable sidecar into an authoritative deletion or empty registry.
VERIFY:
- Instrument file reads: each `*.json` sidecar is read at most once per startup load.
- Instrument `_canonical_or`: 100/200/400 live rows must grow approximately linearly rather than 10^4/4x per doubling.
- Add mixed live/deleted/legacy-tombstone fixtures and assert byte-for-byte-equivalent resulting root sets to current semantics.
- Benchmark 100/200/400/800 valid records; runtime should scale near-linearly.
- Re-run T-703/CORE-004 sidecar validation, wrong-digest quarantine and round-trip coverage.

[P2] [PERF-003] STRONGLY EVIDENCED WASTE saipenview/api.py:_set_cache/_build_ticket_index + saipenview/parser.py:load_project/load_subs
EVIDENCE:
- parser.py:1762-1791 already reads and parses the parent `.saipen/BOARD.md` into `ProjectStatus.board`.
- parser.py:999-1035 calls `load_sub_board()` for every sub; load_sub_board at parser.py:869-876 reads and parses each sub `BOARD.md`.
- load_translate() at parser.py:1039-1052 likewise parses the translate BOARD for counts.
- api.py:912-916 consumes those already-parsed ProjectStatus objects, and `_project_to_dict()` retains parent board counts and sub board counts.
- Immediately after publication, api.py:1008-1012 rebuilds the ticket index for EVERY current root.
- `_build_ticket_index()` at api.py:2803-2822 opens and parses the parent BOARD again and then opens/parses every sub/translate BOARD again through `_search_board_for_tickets()`.
- A direct instrumentation run on the supplied project confirmed the same parent BOARD is independently read by both phases:
  `load_project_root_BOARD_reads=1`
  `ticket_index_root_BOARD_reads=1`
  `same_scan_pipeline_total_root_BOARD_reads=2`
- `_build_ticket_index()` additionally performs `next((p for p in self._projects if p["root"] == root), None)` at api.py:2806. `_set_cache()` invokes it once per root, producing an unnecessary O(N^2) project-list lookup across a full scan.
- The secondary index also has a mismatched eviction lifecycle. Its only root removal is the targeted vanished-project path at api.py:759; `Api.stop()` clears everything at 2611. Full authoritative `_set_cache()` replacements compute removed roots at 1021-1026 only for conformance/parser cache eviction and never remove those roots from `_ticket_index`.
- Deterministic full-scan-removal reproduction seeded one old project with 100 indexed tickets and published a complete empty ScanOutcome:
  `projects_after 0`
  `ticket_index_keys_after ['/tmp/old-project']`
  `retained_ticket_count 100`
- Repeating project churn can therefore retain ticket payloads for every root ever removed by an authoritative full scan until process shutdown.
ISSUE:
The ticket search index is maintained as an independent second parsing/source-of-truth lifecycle instead of being derived from the BOARD data already parsed during project discovery. That causes duplicate filesystem reads/parses on every full scan, an avoidable O(N^2) lookup, and stale ticket-index memory retention when roots disappear through the authoritative scan path.
OPTIMIZE:
Build the searchable ticket snapshot while each BOARD is already parsed and carry that snapshot through ProjectStatus/scan publication, or otherwise hand the already-parsed Board/ticket data directly to the index builder. Reconcile `_ticket_index` atomically against the authoritative current-root set after full scans. For partial scans, preserve index entries beneath unresolved roots and replace/evict only completed-root scopes. If a separate builder must remain, pass it the already-resolved project row/root map instead of linearly searching `self._projects`, and avoid a second disk parse.
GUARDRAIL:
- Quick search must preserve parent/sub/translate ticket IDs, descriptions, sections and `sub_name`.
- Parent or sub BOARD watcher events must still rebuild the affected root immediately.
- Incomplete scans must not erase ticket data for unresolved roots.
- A complete authoritative removal must remove that root's ticket index.
- BOARD parse errors must retain current fail-soft behavior rather than poisoning the whole project list.
VERIFY:
- Instrument `read_doc`: a full scan must perform no second BOARD read solely for ticket indexing.
- With 100/200/400 projects, index construction must avoid N^2 project-list searches.
- Full scan removing a root must leave `set(_ticket_index) <= set(current_project_roots)`.
- Partial scan with an unresolved root must preserve that root's prior index.
- Modify a parent BOARD and a sub BOARD independently and prove quick_search reflects each change after the corresponding watcher refresh.
- Re-add a removed project and prove its index is rebuilt exactly once.

[P2] [PERF-004] STRONGLY EVIDENCED WASTE saipenview/ui/static/app.js:pollAgentsBadge/pollAgentOutput + saipenview/runtime.py:ProcessManager.list_running/get_status
EVIDENCE:
- The main visible-window poll runs every `POLL_MS = 5000` and unconditionally calls `pollAgentsBadge()` after its registry chain at app.js:2297-2340.
- The live-output ticker runs every 750 ms and every 8 ticks, approximately every 6 seconds, calls `pollAgentOutput()` for a running selected agent at app.js:4476-4503.
- `pollAgentOutput()` immediately calls `pollAgentsBadge()` again at app.js:4507-4510.
- `pollAgentsBadge()` at app.js:4422-4433 uses only `agents.length`.
- Despite needing only a count, it calls `SaiApi.list_running_agents()`.
- api.py:3209-3211 delegates that call directly to `ProcessManager.list_running()`.
- runtime.py:1165-1176 enumerates every running root and calls the full `get_status()` for each.
- runtime.py:1124-1138 performs `psutil.Process.cpu_percent()` and `memory_info()` for every running process before returning the full status payload.
- The fleet dashboard legitimately consumes those CPU/RAM values at app.js:4723-4756, but the badge discards all of them.
- With fast responses and 12 running agents, the badge paths alone can issue approximately 12 calls/minute from the 5-second main poll plus 10 calls/minute from the 6-second output-status cadence. At two psutil metric operations per agent, that is approximately `22 * 12 * 2 = 528` process metric calls per minute solely to compute a toolbar count, before the selected agent's own status refreshes or explicit fleet-dashboard refreshes.
ISSUE:
A UI element described in code as a "cheap global concern" invokes the most expensive all-agent status path. The cost scales with agent count and duplicates process introspection at two unrelated polling cadences. This wastes CPU, process API calls, bridge serialization and Python/JS object allocation precisely in the intended multi-agent operating mode.
OPTIMIZE:
Give the badge a count-only backend boundary derived from `_processes` under the existing ProcessManager lock, or provide an explicit lightweight list mode that omits metrics. Keep `list_running_agents()` with full metrics for the fleet dashboard and operations that require roots/run IDs. Coalesce badge refreshes so the output-status cadence does not immediately repeat a recent main-poll count fetch when nothing can have changed.
GUARDRAIL:
- Count semantics must remain exactly the current `status == "running"` behavior.
- The lightweight count path must not call psutil or mutate/prime CPU metric state.
- Fleet dashboard CPU/RAM reporting must continue using the full status path.
- Stop/kill/start transitions must update the badge promptly enough to preserve current UX.
- Do not introduce a second independent lifecycle registry merely to cache the count.
VERIFY:
- Instrument psutil calls: repeated badge polling with N agents must produce zero `cpu_percent`/`memory_info` calls.
- Fleet-dashboard refresh must still collect one full metric snapshot per running agent.
- Verify badge counts across start, successful exit, failed exit and kill transitions.
- With a visible running-agent panel, verify the 5-second and approximately 6-second loops do not duplicate full-fleet metric collection.
- Run the frontend polling tests plus ProcessManager status/list-running regressions in the supported Windows environment.

PERFORMANCE_DONE_WHEN: A timed-out non-cooperative scan root cannot poison usable executor capacity for later healthy scans while global resource growth remains bounded; sidecar cache loading performs one parse and O(N) canonical/index work; full project publication reuses already-parsed BOARD data, avoids N^2 index lookup and evicts removed-root ticket entries; badge polling performs no full-fleet psutil sampling; focused PERF regressions pass and the complete supported-Windows test suite is green.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.